{
  "name": "belacca-status",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/validate-status.mjs status.json --allow-expired && node scripts/validate-slo.mjs slo.json"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
